package com.collections.SetsAndHashSets;

public class Overview {
    /**
     * HASHSET CLASS AND OVERVIEW

     *Hashset cannot contain duplicate values
     * Hashset allows null value

     *Hashset is an unordered collection .it does not maintain the order in which the elements are inserted
     * Hashset internally uses a HashMap to store it elements
     * HashSet is not thread safe if multiple threads try to modify a HashSet at the same time,then the final
     outcome is not- deterministic,you must explicitly synchronized
     */
}
